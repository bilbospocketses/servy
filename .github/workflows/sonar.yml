# --------------------------------------------------------------------------
# SonarCloud Analysis Workflow for Servy
# Description: Performs static code analysis and security scanning.
# Author: Akram El Assas
# --------------------------------------------------------------------------

name: sonar
on:
  push:
    branches:
      - main
  # pull_request:
  #   types: [opened, synchronize, reopened]
  workflow_dispatch: 

permissions:
  contents: read

jobs:
  build:
    name: Build and analyze
    runs-on: windows-latest
    
    env:
      SONAR_SCANNER_VERSION: "11.2.1"

    steps:
      # 1. Checkout code first so the custom local action is available
      # (Note: fetch-depth: 0 is retained for complete Git history)
      - name: Checkout repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 #v4.3.1
        with:
          fetch-depth: 0 
          
      # 2. Setup Java
      - name: Set up JDK 17
        uses: actions/setup-java@c5195efecf7bdfc987ee8bae7a71cb8b11521c00 #v4.7.1
        with:
          java-version: 17
          distribution: 'zulu'

      # 3. CRITICAL: Setup .NET using the custom action to satisfy global.json
      - name: Setup .NET
        uses: ./.github/actions/setup-dotnet

      # 4. Cache Packages
      - name: Cache SonarCloud packages
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 #v4.3.0
        with:
          path: ~\sonar\cache
          key: ${{ runner.os }}-sonar-v1
          restore-keys: ${{ runner.os }}-sonar-

      # 5. Cache Scanner
      - name: Cache SonarCloud scanner
        id: cache-sonar-scanner
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 #v4.3.0
        with:
          path: ${{ runner.temp }}\scanner
          key: ${{ runner.os }}-sonar-scanner-${{ env.SONAR_SCANNER_VERSION }}
          restore-keys: ${{ runner.os }}-sonar-scanner-

      # 6. Install / Update Scanner
      - name: Install SonarCloud scanner
        if: steps.cache-sonar-scanner.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          if (!(Test-Path ${{ runner.temp }}\scanner)) { New-Item -Path ${{ runner.temp }}\scanner -ItemType Directory }
          # Utilizing 'update' ensures version switches don't crash when versionless fallbacks populate the tool-path
          dotnet tool update dotnet-sonarscanner --version $env:SONAR_SCANNER_VERSION --tool-path ${{ runner.temp }}\scanner

      # 7. Build and Analyze
      - name: Build and analyze
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        shell: pwsh
        run: |
          ${{ runner.temp }}\scanner\dotnet-sonarscanner begin `
            /k:"aelassas_servy" `
            /o:"aelassas" `
            /d:sonar.token="$env:SONAR_TOKEN" `
            /d:sonar.host.url="https://sonarcloud.io"
          
          if ($LASTEXITCODE -ne 0) { throw "sonarscanner begin failed (exit $LASTEXITCODE)" }

          dotnet build Servy.sln --configuration Release
          $buildExit = $LASTEXITCODE

          ${{ runner.temp }}\scanner\dotnet-sonarscanner end /d:sonar.token="$env:SONAR_TOKEN"
          $endExit = $LASTEXITCODE

          if ($buildExit -ne 0) { throw "dotnet build failed (exit $buildExit)" }
          if ($endExit  -ne 0) { throw "sonarscanner end failed (exit $endExit)" }