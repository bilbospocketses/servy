<Window x:Class="Servy.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Servy"
        xmlns:resx="clr-namespace:Servy.Resources"
        xmlns:viewmodels="clr-namespace:Servy.ViewModels"
        xmlns:converters="clr-namespace:Servy.UI.Converters;assembly=Servy.UI"
        xmlns:helpers="clr-namespace:Servy.Helpers"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        Title="Servy"
        MinWidth="640"
        MinHeight="760"
        SizeToContent="WidthAndHeight"
        ResizeMode="CanMinimize"
        AutomationProperties.Name="Servy Configuration Window"
        AutomationProperties.AutomationId="ConfigMainWindow"
        AutomationProperties.HelpText="Main window for creating and configuring a Servy Windows service."
        >
    <Window.Resources>
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter" />
    </Window.Resources>
    <DockPanel>

        <Menu DockPanel.Dock="Top" Height="28"
              AutomationProperties.Name="Main Menu"
              AutomationProperties.AutomationId="MainMenu"
              AutomationProperties.HelpText="Application menu for exporting, importing, and help options.">

            <MenuItem Header="{x:Static resx:Strings.Menu_Export}"
                      AutomationProperties.Name="{x:Static resx:Strings.Menu_Export}"
                      AutomationProperties.AutomationId="MenuExport"
                      AutomationProperties.HelpText="Export service configuration menu.">
                <MenuItem Header="{x:Static resx:Strings.Menu_ExportXml}" Command="{Binding ExportXmlCommand}"
                          AutomationProperties.Name="{x:Static resx:Strings.Menu_ExportXml}"
                          AutomationProperties.AutomationId="MenuExportXml"
                          AutomationProperties.HelpText="Export configuration to an XML file."/>
                <MenuItem Header="{x:Static resx:Strings.Menu_ExportJson}" Command="{Binding ExportJsonCommand}"
                          AutomationProperties.Name="{x:Static resx:Strings.Menu_ExportJson}"
                          AutomationProperties.AutomationId="MenuExportJson"
                          AutomationProperties.HelpText="Export configuration to a JSON file."/>
            </MenuItem>

            <MenuItem Header="{x:Static resx:Strings.Menu_Import}"
                      AutomationProperties.Name="{x:Static resx:Strings.Menu_Import}"
                      AutomationProperties.AutomationId="MenuImport"
                      AutomationProperties.HelpText="Import service configuration menu.">
                <MenuItem Header="{x:Static resx:Strings.Menu_ImportXml}" Command="{Binding ImportXmlCommand}"
                          AutomationProperties.Name="{x:Static resx:Strings.Menu_ImportXml}"
                          AutomationProperties.AutomationId="MenuImportXml"
                          AutomationProperties.HelpText="Import configuration from an XML file."/>
                <MenuItem Header="{x:Static resx:Strings.Menu_ImportJson}" Command="{Binding ImportJsonCommand}"
                          AutomationProperties.Name="{x:Static resx:Strings.Menu_ImportJson}"
                          AutomationProperties.AutomationId="MenuImportJson"
                          AutomationProperties.HelpText="Import configuration from a JSON file."/>
            </MenuItem>

            <MenuItem Header="{x:Static resx:Strings.Menu_Manager}" Command="{Binding ManagerCommand}" IsEnabled="{Binding IsManagerAppAvailable}"
                      AutomationProperties.Name="{x:Static resx:Strings.Menu_Manager}"
                      AutomationProperties.AutomationId="MenuManager"
                      AutomationProperties.HelpText="Open the Servy Manager application."/>

            <MenuItem Header="{x:Static resx:Strings.Menu_ClearAll}" Command="{Binding ClearFormCommand}"
                      AutomationProperties.Name="{x:Static resx:Strings.Menu_ClearAll}"
                      AutomationProperties.AutomationId="MenuClearAll"
                      AutomationProperties.HelpText="Clear all fields in the configuration form."/>

            <MenuItem Header="{x:Static resx:Strings.Menu_Help}"
                      AutomationProperties.Name="{x:Static resx:Strings.Menu_Help}"
                      AutomationProperties.AutomationId="MenuHelp"
                      AutomationProperties.HelpText="Help and documentation menu.">
                <MenuItem Header="{x:Static resx:Strings.Menu_Doc}" Command="{Binding OpenDocumentationCommand}"
                          AutomationProperties.Name="{x:Static resx:Strings.Menu_Doc}"
                          AutomationProperties.AutomationId="MenuDoc"
                          AutomationProperties.HelpText="Open the official Servy documentation."/>
                <MenuItem Header="{x:Static resx:Strings.Menu_CheckUpdates}" Command="{Binding CheckUpdatesCommand}"
                          AutomationProperties.Name="{x:Static resx:Strings.Menu_CheckUpdates}"
                          AutomationProperties.AutomationId="MenuCheckUpdates"
                          AutomationProperties.HelpText="Check online for application updates."/>
                <MenuItem Header="{x:Static resx:Strings.Menu_About}" Command="{Binding OpenAboutDialogCommand}"
                          AutomationProperties.Name="{x:Static resx:Strings.Menu_About}"
                          AutomationProperties.AutomationId="MenuAbout"
                          AutomationProperties.HelpText="Show information about the Servy application."/>
            </MenuItem>
        </Menu>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TabControl Grid.Row="0"
                        AutomationProperties.Name="Configuration Tabs"
                        AutomationProperties.AutomationId="ConfigurationTabControl"
                        AutomationProperties.HelpText="Navigate between different configuration sections for the service.">

                <TabItem Header="{x:Static resx:Strings.Label_MainTab}"
                         AutomationProperties.Name="{x:Static resx:Strings.Label_MainTab}"
                         AutomationProperties.AutomationId="TabItemMain"
                         AutomationProperties.HelpText="General configuration options for the service.">
                    <Grid Margin="20" HorizontalAlignment="Center" VerticalAlignment="Top" MinHeight="520">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="340"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="{x:Static resx:Strings.Label_ServiceName}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" 
                                 Margin="0,5"
                                 Text="{Binding ServiceName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="{x:Static resx:Strings.Label_ServiceName}"
                                 AutomationProperties.AutomationId="TextBoxServiceName"
                                 AutomationProperties.HelpText="Enter the system name of the service."/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="{x:Static resx:Strings.Label_ServiceDisplayName}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2"
                                 Margin="0,5"
                                 Text="{Binding ServiceDisplayName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="{x:Static resx:Strings.Label_ServiceDisplayName}"
                                 AutomationProperties.AutomationId="TextBoxServiceDisplayName"
                                 AutomationProperties.HelpText="Enter the display name of the service."/>
                        <TextBlock Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2"
                                   Text="{x:Static resx:Strings.Info_ServiceDisplayName}" 
                                   FontStyle="Normal" 
                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                   TextWrapping="Wrap"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="{x:Static resx:Strings.Label_ServiceDescription}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2"
                                 Margin="0,5"
                                 Text="{Binding ServiceDescription, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="{x:Static resx:Strings.Label_ServiceDescription}"
                                 AutomationProperties.AutomationId="TextBoxServiceDescription"
                                 AutomationProperties.HelpText="Enter the description of the service."/>

                        <TextBlock Grid.Row="4" Grid.Column="0" Text="{x:Static resx:Strings.Label_ProcessPath}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="4" Grid.Column="1" 
                                 Margin="0,5"
                                 Text="{Binding ProcessPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="{x:Static resx:Strings.Label_ProcessPath}"
                                 AutomationProperties.AutomationId="TextBoxProcessPath"
                                 AutomationProperties.HelpText="Enter the executable path for the service."/>
                        <Button Grid.Row="4" Grid.Column="2" Content="{x:Static resx:Strings.Button_Browse}" Command="{Binding BrowseProcessPathCommand}" Margin="5,5,0,5"
                                AutomationProperties.Name="Browse Process Path"
                                AutomationProperties.AutomationId="ButtonBrowseProcessPath"
                                AutomationProperties.HelpText="Browse to select the executable process path."/>
                        <TextBlock Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="2"
                                   Text="{x:Static resx:Strings.Info_ProcessPath}" 
                                   FontStyle="Normal" 
                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                   TextWrapping="Wrap"/>

                        <TextBlock Grid.Row="6" Grid.Column="0" Text="{x:Static resx:Strings.Label_StartupDirectory}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="6" Grid.Column="1" 
                                 Margin="0,5"
                                 Text="{Binding StartupDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="{x:Static resx:Strings.Label_StartupDirectory}"
                                 AutomationProperties.AutomationId="TextBoxStartupDirectory"
                                 AutomationProperties.HelpText="Enter the startup working directory."/>
                        <Button Grid.Row="6" Grid.Column="2" Content="{x:Static resx:Strings.Button_Browse}" Command="{Binding BrowseStartupDirectoryCommand}" Margin="5,5,0,5"
                                AutomationProperties.Name="Browse Startup Directory"
                                AutomationProperties.AutomationId="ButtonBrowseStartupDirectory"
                                AutomationProperties.HelpText="Browse to select the startup directory."/>
                        <TextBlock Grid.Row="7" Grid.Column="1" Grid.ColumnSpan="2"
                                   Text="{x:Static resx:Strings.Info_StartupDirectory}" 
                                   FontStyle="Normal" 
                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                   TextWrapping="Wrap"/>

                        <TextBlock Grid.Row="8" Grid.Column="0" Text="{x:Static resx:Strings.Label_ProcessParameters}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="8" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,5" 
                                 Text="{Binding ProcessParameters, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 AcceptsReturn="False" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Height="80"
                                 AutomationProperties.Name="{x:Static resx:Strings.Label_ProcessParameters}"
                                 AutomationProperties.AutomationId="TextBoxProcessParameters"
                                 AutomationProperties.HelpText="Enter the command line parameters for the process."/>
                        <TextBlock Grid.Row="9" Grid.Column="1" Grid.ColumnSpan="2"
                                   Text="{x:Static resx:Strings.Info_ProcessParameters}" 
                                   FontStyle="Normal" 
                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                   TextWrapping="Wrap"/>

                        <TextBlock Grid.Row="10" Grid.Column="0" Text="{x:Static resx:Strings.Label_StartupType}" Margin="0,5,10,5"/>
                        <ComboBox Grid.Row="10" Grid.Column="1" Grid.ColumnSpan="2"
                                  Margin="0,5" 
                                  ItemsSource="{Binding StartupTypes}"
                                  DisplayMemberPath="DisplayName"
                                  SelectedValue="{Binding SelectedStartupType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  SelectedValuePath="Value"
                                  AutomationProperties.Name="{x:Static resx:Strings.Label_StartupType}"
                                  AutomationProperties.AutomationId="ComboBoxStartupType"
                                  AutomationProperties.HelpText="Select the service startup type."/>

                        <TextBlock Grid.Row="11" Grid.Column="0" Text="{x:Static resx:Strings.Label_ProcessPriority}" Margin="0,5,10,5"/>
                        <ComboBox Grid.Row="11" Grid.Column="1" Grid.ColumnSpan="2"
                                  Margin="0,5" 
                                  ItemsSource="{Binding ProcessPriorities}"
                                  DisplayMemberPath="DisplayName"
                                  SelectedValue="{Binding SelectedProcessPriority, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  SelectedValuePath="Value"
                                  AutomationProperties.Name="{x:Static resx:Strings.Label_ProcessPriority}"
                                  AutomationProperties.AutomationId="ComboBoxProcessPriority"
                                  AutomationProperties.HelpText="Select the process priority level."/>

                        <TextBlock Grid.Row="12" Grid.Column="0" Text="{x:Static resx:Strings.Label_StartTimeout}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="12" Grid.Column="1" Grid.ColumnSpan="2"
                                 Margin="0,5"
                                 Text="{Binding StartTimeout, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="{x:Static resx:Strings.Label_StartTimeout}"
                                 AutomationProperties.AutomationId="TextBoxStartTimeout"
                                 AutomationProperties.HelpText="Enter the timeout in seconds for starting the service."/>
                        <TextBlock Grid.Row="13" Grid.Column="1" Grid.ColumnSpan="2"
                                   Text="{x:Static resx:Strings.Info_StartTimeout}" 
                                   FontStyle="Normal" 
                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                   TextWrapping="Wrap"/>

                        <TextBlock Grid.Row="14" Grid.Column="0" Text="{x:Static resx:Strings.Label_StopTimeout}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="14" Grid.Column="1" Grid.ColumnSpan="2"
                                 Margin="0,5"
                                 Text="{Binding StopTimeout, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="{x:Static resx:Strings.Label_StopTimeout}"
                                 AutomationProperties.AutomationId="TextBoxStopTimeout"
                                 AutomationProperties.HelpText="Enter the timeout in seconds for stopping the service."/>
                        <TextBlock Grid.Row="15" Grid.Column="1" Grid.ColumnSpan="2"
                                   Text="{x:Static resx:Strings.Info_StopTimeout}" 
                                   FontStyle="Normal" 
                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                   TextWrapping="Wrap"/>
                        <CheckBox Grid.Row="16" Grid.Column="1" Grid.ColumnSpan="2"
                                  IsChecked="{Binding EnableConsoleUI, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  Margin="0,10"
                                  AutomationProperties.Name="{x:Static resx:Strings.Label_EnableConsoleUI}"
                                  AutomationProperties.AutomationId="CheckBoxEnableConsoleUI"
                                  AutomationProperties.HelpText="Check to enable console UI support.">
                            <CheckBox.Content>
                                <TextBlock Text="{x:Static resx:Strings.Label_EnableConsoleUI}" TextWrapping="Wrap" />
                            </CheckBox.Content>
                        </CheckBox>
                    </Grid>
                </TabItem>

                <TabItem Header="{x:Static resx:Strings.Label_LoggingTab}"
                         AutomationProperties.Name="{x:Static resx:Strings.Label_LoggingTab}"
                         AutomationProperties.AutomationId="TabItemLogging"
                         AutomationProperties.HelpText="Configure standard output and error logging settings.">
                    <Grid Margin="20" HorizontalAlignment="Center" VerticalAlignment="Top" MinHeight="520">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="340"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="{x:Static resx:Strings.Label_StdoutPath}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="0" Grid.Column="1" 
                                 Margin="0,5"
                                 Text="{Binding StdoutPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="{x:Static resx:Strings.Label_StdoutPath}"
                                 AutomationProperties.AutomationId="TextBoxStdoutPath"
                                 AutomationProperties.HelpText="Enter the path for the standard output log file."/>
                        <Button Grid.Row="0" Grid.Column="2" Content="{x:Static resx:Strings.Button_Browse}" Command="{Binding BrowseStdoutPathCommand}" Margin="5,5,0,5"
                                AutomationProperties.Name="Browse Standard Output Path"
                                AutomationProperties.AutomationId="ButtonBrowseStdout"
                                AutomationProperties.HelpText="Browse to select the standard output log file."/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="{x:Static resx:Strings.Label_StderrPath}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="1" Grid.Column="1" 
                                 Margin="0,5"
                                 Text="{Binding StderrPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="{x:Static resx:Strings.Label_StderrPath}"
                                 AutomationProperties.AutomationId="TextBoxStderrPath"
                                 AutomationProperties.HelpText="Enter the path for the standard error log file."/>
                        <Button Grid.Row="1" Grid.Column="2" Content="{x:Static resx:Strings.Button_Browse}" Command="{Binding BrowseStderrPathCommand}" Margin="5,5,0,5"
                                AutomationProperties.Name="Browse Standard Error Path"
                                AutomationProperties.AutomationId="ButtonBrowseStderr"
                                AutomationProperties.HelpText="Browse to select the standard error log file."/>

                        <CheckBox Grid.Row="2" Grid.Column="1"
                                  Content="{x:Static resx:Strings.Label_EnableSizeRotation}"
                                  IsChecked="{Binding EnableSizeRotation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  Margin="0,5"
                                  AutomationProperties.Name="{x:Static resx:Strings.Label_EnableSizeRotation}"
                                  AutomationProperties.AutomationId="CheckBoxEnableSizeRotation"
                                  AutomationProperties.HelpText="Check to enable log rotation based on file size."/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="{x:Static resx:Strings.Label_RotationSize}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="3" Grid.Column="1" 
                                 Margin="0,5"
                                 IsEnabled="{Binding EnableSizeRotation}"
                                 Text="{Binding RotationSize, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="{x:Static resx:Strings.Label_RotationSize}"
                                 AutomationProperties.AutomationId="TextBoxRotationSize"
                                 AutomationProperties.HelpText="Enter the maximum log file size before rotation."/>
                        <TextBlock Grid.Row="3" Grid.Column="2" Text="{x:Static resx:Strings.Label_RotationSizeUnit}" VerticalAlignment="Center" Margin="5,5,0,5"/>

                        <CheckBox Grid.Row="4" Grid.Column="1"
                                  Content="{x:Static resx:Strings.Label_EnableDateRotation}"
                                  IsChecked="{Binding EnableDateRotation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  Margin="0,5"
                                  AutomationProperties.Name="{x:Static resx:Strings.Label_EnableDateRotation}"
                                  AutomationProperties.AutomationId="CheckBoxEnableDateRotation"
                                  AutomationProperties.HelpText="Check to enable log rotation based on dates."/>

                        <TextBlock Grid.Row="5" Grid.Column="0" Text="{x:Static resx:Strings.Label_DateRotationType}" Margin="0,5,10,5"/>
                        <ComboBox Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="2" 
                                  Margin="0,5"
                                  ItemsSource="{Binding DateRotationTypes}"
                                  DisplayMemberPath="DisplayName"
                                  SelectedValue="{Binding SelectedDateRotationType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  SelectedValuePath="Value"
                                  IsEnabled="{Binding EnableDateRotation}"
                                  AutomationProperties.Name="{x:Static resx:Strings.Label_DateRotationType}"
                                  AutomationProperties.AutomationId="ComboBoxDateRotationType"
                                  AutomationProperties.HelpText="Select the interval for date-based log rotation."/>
                        <TextBlock Grid.Row="6" Grid.Column="1" Grid.ColumnSpan="2"
                                   Text="{x:Static resx:Strings.Info_DateRotation}" 
                                   FontStyle="Normal" 
                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                   TextWrapping="Wrap"/>

                        <TextBlock Grid.Row="7" Grid.Column="0" Text="{x:Static resx:Strings.Label_MaxRotations}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="7" Grid.Column="1" Grid.ColumnSpan="2" 
                                 Margin="0,5"
                                 IsEnabled="{Binding EnableRotation}"
                                 Text="{Binding MaxRotations, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="{x:Static resx:Strings.Label_MaxRotations}"
                                 AutomationProperties.AutomationId="TextBoxMaxRotations"
                                 AutomationProperties.HelpText="Enter the maximum number of rotated logs to keep."/>
                        <TextBlock Grid.Row="8" Grid.Column="1" Grid.ColumnSpan="2"
                                   Text="{x:Static resx:Strings.Info_MaxRotations}" 
                                   FontStyle="Normal" 
                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                   TextWrapping="Wrap"/>

                        <CheckBox Grid.Row="9" Grid.Column="1"
                                  Content="{x:Static resx:Strings.Label_UseLocalTimeForRotation}"
                                  IsChecked="{Binding UseLocalTimeForRotation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  Margin="0,5"
                                  AutomationProperties.Name="{x:Static resx:Strings.Label_UseLocalTimeForRotation}"
                                  AutomationProperties.AutomationId="CheckBoxUseLocalTimeForRotation"
                                  AutomationProperties.HelpText="Check to use local time instead of UTC for log rotations."/>

                        <CheckBox Grid.Row="10" Grid.Column="1"
                                  Content="{x:Static resx:Strings.Label_EnableDebugLogs}"
                                  IsChecked="{Binding EnableDebugLogs, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  Margin="0,5"
                                  AutomationProperties.Name="{x:Static resx:Strings.Label_EnableDebugLogs}"
                                  AutomationProperties.AutomationId="CheckBoxEnableDebugLogs"
                                  AutomationProperties.HelpText="Check to enable diagnostic debug logging."/>
                        <TextBlock Grid.Row="11" Grid.Column="1" Grid.ColumnSpan="2"
                                   Text="{x:Static resx:Strings.Info_EnableDebugLogs}" 
                                   FontStyle="Normal" 
                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                   TextWrapping="Wrap"/>
                    </Grid>
                </TabItem>

                <TabItem Header="{x:Static resx:Strings.Label_RecoveryTab}"
                         AutomationProperties.Name="{x:Static resx:Strings.Label_RecoveryTab}"
                         AutomationProperties.AutomationId="TabItemRecovery"
                         AutomationProperties.HelpText="Configure automated health checks and recovery actions.">
                    <Grid Margin="20" HorizontalAlignment="Center" VerticalAlignment="Top" MinHeight="520">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="340"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>

                        <CheckBox Grid.Row="0" 
                                  Grid.Column="1" 
                                  Content="{x:Static resx:Strings.Label_EnableHealthMonitoring}"
                                  IsChecked="{Binding EnableHealthMonitoring, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  Margin="0,5"
                                  AutomationProperties.Name="{x:Static resx:Strings.Label_EnableHealthMonitoring}"
                                  AutomationProperties.AutomationId="CheckBoxEnableHealthMonitoring"
                                  AutomationProperties.HelpText="Check to enable background health monitoring for this service."/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="{x:Static resx:Strings.Label_HeartbeatInterval}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="1" Grid.Column="1" 
                                 Margin="0,5"
                                 IsEnabled="{Binding EnableHealthMonitoring}"
                                 Text="{Binding HeartbeatInterval, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="{x:Static resx:Strings.Label_HeartbeatInterval}"
                                 AutomationProperties.AutomationId="TextBoxHeartbeatInterval"
                                 AutomationProperties.HelpText="Enter the health check heartbeat interval in seconds."/>
                        <TextBlock Grid.Row="1" Grid.Column="2" Text="{x:Static resx:Strings.Label_Seconds}" VerticalAlignment="Center" Margin="5,5,0,5"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="{x:Static resx:Strings.Label_MaxFailedChecks}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="2" Grid.Column="1" 
                                 Margin="0,5"
                                 IsEnabled="{Binding EnableHealthMonitoring}"
                                 Text="{Binding MaxFailedChecks, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="{x:Static resx:Strings.Label_MaxFailedChecks}"
                                 AutomationProperties.AutomationId="TextBoxMaxFailedChecks"
                                 AutomationProperties.HelpText="Enter the maximum number of failed checks before triggering recovery."/>
                        <TextBlock Grid.Row="2" Grid.Column="2" Text="{x:Static resx:Strings.Label_Attempts}" VerticalAlignment="Center" Margin="5,5,0,5"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="{x:Static resx:Strings.Label_RecoveryAction}" Margin="0,5,10,5"/>
                        <ComboBox Grid.Row="3" 
                                  Grid.Column="1" Grid.ColumnSpan="2"
                                  Margin="0,5"
                                  ItemsSource="{Binding RecoveryActions, UpdateSourceTrigger=PropertyChanged}"
                                  DisplayMemberPath="DisplayName"
                                  SelectedValue="{Binding SelectedRecoveryAction, Mode=TwoWay}"
                                  SelectedValuePath="Value"
                                  IsEnabled="{Binding EnableHealthMonitoring}"
                                  AutomationProperties.Name="{x:Static resx:Strings.Label_RecoveryAction}"
                                  AutomationProperties.AutomationId="ComboBoxRecoveryAction"
                                  AutomationProperties.HelpText="Select the action to take when the service fails health checks."/>

                        <TextBlock Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="3"
                                  Text="{x:Static resx:Strings.Info_RecoveryAction}" 
                                  FontStyle="Normal" Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" TextWrapping="Wrap" Margin="0,0,0,10"/>

                        <CheckBox Grid.Row="5" Grid.Column="1"
                                  Content="{x:Static resx:Strings.Label_RecoveryOnCleanExit}"
                                  IsEnabled="{Binding EnableHealthMonitoring}"
                                  IsChecked="{Binding RecoveryOnCleanExit, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  Margin="0,5"
                                  AutomationProperties.Name="{x:Static resx:Strings.Label_RecoveryOnCleanExit}"
                                  AutomationProperties.AutomationId="CheckBoxRecoveryOnCleanExit"
                                  AutomationProperties.HelpText="Check to run recovery action even if the process exits successfully."/>
                        <TextBlock Grid.Row="6" Grid.Column="1" Grid.ColumnSpan="2"
                                   Text="{x:Static resx:Strings.Info_RecoveryOnCleanExit}" 
                                   FontStyle="Normal" 
                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                   TextWrapping="Wrap"/>

                        <TextBlock Grid.Row="7" Grid.Column="0" Text="{x:Static resx:Strings.Label_MaxRestartAttempts}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="7" Grid.Column="1" 
                                 Margin="0,5"
                                 IsEnabled="{Binding EnableHealthMonitoring}"
                                 Text="{Binding MaxRestartAttempts, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="{x:Static resx:Strings.Label_MaxRestartAttempts}"
                                 AutomationProperties.AutomationId="TextBoxMaxRestartAttempts"
                                 AutomationProperties.HelpText="Enter the maximum number of restart attempts."/>
                        <TextBlock Grid.Row="7" Grid.Column="2" Text="{x:Static resx:Strings.Label_Attempts}" VerticalAlignment="Center" Margin="5,5,0,5"/>
                        <TextBlock Grid.Row="8" Grid.Column="1" Grid.ColumnSpan="2"
                                   Text="{x:Static resx:Strings.Info_MaxRestartAttempts}" 
                                   FontStyle="Normal" 
                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                   TextWrapping="Wrap" 
                                   Margin="0,0,0,5"/>

                        <TextBlock Grid.Row="9" Grid.Column="1" Grid.ColumnSpan="3" Text="{x:Static resx:Strings.Info_FailureProgram}" FontStyle="Normal" Foreground="Black" TextWrapping="Wrap" Margin="0,0,0,5"/>

                        <TextBlock Grid.Row="10" Grid.Column="0" Text="{x:Static resx:Strings.Label_ProcessPathNotRequired}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="10" Grid.Column="1" 
                                 Margin="0,5"
                                 Text="{Binding FailureProgramPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="Failure Program Path"
                                 AutomationProperties.AutomationId="TextBoxFailureProgramPath"
                                 AutomationProperties.HelpText="Enter the path to the executable to run upon failure."/>
                        <Button Grid.Row="10" Grid.Column="2" Content="{x:Static resx:Strings.Button_Browse}" Command="{Binding BrowseFailureProgramPathCommand}" Margin="5,5,0,5"
                                AutomationProperties.Name="Browse Failure Program Path"
                                AutomationProperties.AutomationId="ButtonBrowseFailureProgramPath"
                                AutomationProperties.HelpText="Browse to select the failure program executable."/>
                        <TextBlock Grid.Row="11" Grid.Column="1" Grid.ColumnSpan="2"
                                   Text="{x:Static resx:Strings.Info_ProcessPath}" 
                                   FontStyle="Normal" 
                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                   TextWrapping="Wrap"/>

                        <TextBlock Grid.Row="12" Grid.Column="0" Text="{x:Static resx:Strings.Label_StartupDirectory}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="12" Grid.Column="1" 
                                 Margin="0,5"
                                 Text="{Binding FailureProgramStartupDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="Failure Program Startup Directory"
                                 AutomationProperties.AutomationId="TextBoxFailureProgramStartupDir"
                                 AutomationProperties.HelpText="Enter the startup directory for the failure program."/>
                        <Button Grid.Row="12" Grid.Column="2" Content="{x:Static resx:Strings.Button_Browse}" Command="{Binding BrowseFailureProgramStartupDirectoryCommand}" Margin="5,5,0,5"
                                AutomationProperties.Name="Browse Failure Program Startup Directory"
                                AutomationProperties.AutomationId="ButtonBrowseFailureProgramDir"
                                AutomationProperties.HelpText="Browse to select the failure program startup directory."/>
                        <TextBlock Grid.Row="13"  Grid.Column="1" Grid.ColumnSpan="2" Text="{x:Static resx:Strings.Info_FailureProgramStartupDirectory}" FontStyle="Normal" Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" TextWrapping="Wrap"/>
                        <TextBlock Grid.Row="14" Grid.Column="1" Grid.ColumnSpan="2"
                                   Text="{x:Static resx:Strings.Info_StartupDirectory}" 
                                   FontStyle="Normal" 
                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                   TextWrapping="Wrap"/>

                        <TextBlock Grid.Row="15" Grid.Column="0" Text="{x:Static resx:Strings.Label_ProcessParameters}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="15" Grid.Column="1" Grid.ColumnSpan="2"
                                 Margin="0,5"
                                 Text="{Binding FailureProgramParameters, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="Failure Program Parameters"
                                 AutomationProperties.AutomationId="TextBoxFailureProgramParams"
                                 AutomationProperties.HelpText="Enter command line parameters for the failure program."/>
                        <TextBlock Grid.Row="16" Grid.Column="1" Grid.ColumnSpan="2"
                                   Text="{x:Static resx:Strings.Info_ProcessParameters}" 
                                   FontStyle="Normal" 
                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                   TextWrapping="Wrap"/>
                    </Grid>
                </TabItem>

                <TabItem Header="{x:Static resx:Strings.Label_AdvancedTab}"
                         AutomationProperties.Name="{x:Static resx:Strings.Label_AdvancedTab}"
                         AutomationProperties.AutomationId="TabItemAdvanced"
                         AutomationProperties.HelpText="Configure environment variables and service dependencies.">
                    <Grid Margin="20" HorizontalAlignment="Center" VerticalAlignment="Top" MinHeight="520">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>

                        </Grid.RowDefinitions>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="340"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="{x:Static resx:Strings.Label_EnvironmentVariables}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding EnvironmentVariables, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" Grid.ColumnSpan="2" 
                                 AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Height="120"
                                 AutomationProperties.Name="{x:Static resx:Strings.Label_EnvironmentVariables}"
                                 AutomationProperties.AutomationId="TextBoxEnvVars"
                                 AutomationProperties.HelpText="Enter environment variables for the service."/>
                        <TextBlock Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2"
                                   Text="{x:Static resx:Strings.Info_EnvironmentVariables}" 
                                   FontStyle="Normal" 
                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                   TextWrapping="Wrap"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="{x:Static resx:Strings.Label_ServiceDependencies}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding ServiceDependencies, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Margin="0,5" Grid.ColumnSpan="2" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Height="120"
                                 AutomationProperties.Name="{x:Static resx:Strings.Label_ServiceDependencies}"
                                 AutomationProperties.AutomationId="TextBoxServiceDependencies"
                                 AutomationProperties.HelpText="Enter Windows service dependencies."/>
                        <TextBlock Grid.Row="4"  Grid.Column="1" Grid.ColumnSpan="2" 
                                   Text="{x:Static resx:Strings.Info_ServiceDependencies}" FontStyle="Normal" Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" 
                                   TextWrapping="Wrap"/>

                    </Grid>
                </TabItem>

                <TabItem Header="{x:Static resx:Strings.Label_LogOnTab}"
                         AutomationProperties.Name="{x:Static resx:Strings.Label_LogOnTab}"
                         AutomationProperties.AutomationId="TabItemLogOn"
                         AutomationProperties.HelpText="Configure the user account context for the service.">
                    <Grid Margin="20" HorizontalAlignment="Center" VerticalAlignment="Top" MinHeight="520">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="340"/>
                        </Grid.ColumnDefinitions>

                        <RadioButton Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,0,10"
                                     Content="{x:Static resx:Strings.Label_RunAsLocalSystem}" 
                                     IsChecked="{Binding RunAsLocalSystem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     AutomationProperties.Name="{x:Static resx:Strings.Label_RunAsLocalSystem}"
                                     AutomationProperties.AutomationId="RadioButtonLocalSystem"
                                     AutomationProperties.HelpText="Run the service as the built-in Local System account."/>

                        <RadioButton Grid.Row="1" Grid.Column="0"
                                     Content="{x:Static resx:Strings.Label_UserAccount}"
                                     IsChecked="{Binding RunAsLocalSystem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource InverseBooleanConverter}}"
                                     AutomationProperties.Name="{x:Static resx:Strings.Label_UserAccount}"
                                     AutomationProperties.AutomationId="RadioButtonUserAccount"
                                     AutomationProperties.HelpText="Run the service under a specific user account."/>

                        <TextBox Grid.Row="1" Grid.Column="1" 
                                 Margin="0,0,0,5"
                                 IsEnabled="{Binding RunAsLocalSystem, Converter={StaticResource InverseBooleanConverter}}"
                                 Text="{Binding UserAccount, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="User Account Name"
                                 AutomationProperties.AutomationId="TextBoxUserAccount"
                                 AutomationProperties.HelpText="Enter the username for the service account."/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                                   Text="{x:Static resx:Strings.Info_UserAccount}" 
                                   FontStyle="Normal" Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" TextWrapping="Wrap" Margin="0,0,0,10"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="{x:Static resx:Strings.Label_Password}" Margin="0,5,10,5" VerticalAlignment="Center"/>

                        <PasswordBox Grid.Row="3" Grid.Column="1"
                                  Margin="0,0,0,10"
                                  IsEnabled="{Binding RunAsLocalSystem, Converter={StaticResource InverseBooleanConverter}}"
                                  helpers:PasswordBoxHelper.BoundPassword="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  AutomationProperties.Name="{x:Static resx:Strings.Label_Password}"
                                  AutomationProperties.AutomationId="PasswordBoxUser"
                                  AutomationProperties.HelpText="Enter the password for the service account."/>

                        <TextBlock Grid.Row="4" Grid.Column="0" Text="{x:Static resx:Strings.Label_ConfirmPassword}" Margin="0,5,10,5" VerticalAlignment="Center"/>

                        <PasswordBox Grid.Row="4" Grid.Column="1"
                                 Margin="0,0,0,10"
                                 IsEnabled="{Binding RunAsLocalSystem, Converter={StaticResource InverseBooleanConverter}}"
                                 helpers:PasswordBoxHelper.BoundPassword="{Binding ConfirmPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 AutomationProperties.Name="{x:Static resx:Strings.Label_ConfirmPassword}"
                                 AutomationProperties.AutomationId="PasswordBoxConfirm"
                                 AutomationProperties.HelpText="Confirm the password for the service account."/>

                        <TextBlock Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2"
                                   Text="{x:Static resx:Strings.Info_UserAccountRights}" 
                                   FontStyle="Normal" Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" TextWrapping="Wrap" Margin="0,0,0,10"/>
                    </Grid>
                </TabItem>

                <TabItem Header="{x:Static resx:Strings.Label_PreLaunchTab}"
                         AutomationProperties.Name="{x:Static resx:Strings.Label_PreLaunchTab}"
                         AutomationProperties.AutomationId="TabItemPreLaunch"
                         AutomationProperties.HelpText="Configure a script or process to run before the service starts.">
                    <Grid Margin="20" HorizontalAlignment="Center" VerticalAlignment="Top" MinHeight="520">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="340"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0"  Grid.Column="1" Grid.ColumnSpan="3" Text="{x:Static resx:Strings.Info_PreLaunch}" FontStyle="Normal" Foreground="Black" TextWrapping="Wrap" Margin="5,0,0,5"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="{x:Static resx:Strings.Label_ProcessPathNotRequired}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="1" Grid.Column="1" 
                                 Margin="0,5"
                                 Text="{Binding PreLaunchExecutablePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="Pre-Launch Process Path"
                                 AutomationProperties.AutomationId="TextBoxPreLaunchProcessPath"
                                 AutomationProperties.HelpText="Enter the pre-launch executable path."/>
                        <Button Grid.Row="1" Grid.Column="2" Content="{x:Static resx:Strings.Button_Browse}" Command="{Binding BrowsePreLaunchProcessPathCommand}" Margin="5,5,0,5"
                                AutomationProperties.Name="Browse Pre-Launch Process Path"
                                AutomationProperties.AutomationId="ButtonBrowsePreLaunchProcessPath"
                                AutomationProperties.HelpText="Browse to select the pre-launch executable."/>
                        <TextBlock Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2"
                                   Text="{x:Static resx:Strings.Info_ProcessPath}" 
                                   FontStyle="Normal" 
                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                   TextWrapping="Wrap"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="{x:Static resx:Strings.Label_StartupDirectory}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="3" Grid.Column="1" 
                                 Margin="0,5"
                                 Text="{Binding PreLaunchStartupDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="Pre-Launch Startup Directory"
                                 AutomationProperties.AutomationId="TextBoxPreLaunchStartupDir"
                                 AutomationProperties.HelpText="Enter the startup directory for the pre-launch process."/>
                        <Button Grid.Row="3" Grid.Column="2" Content="{x:Static resx:Strings.Button_Browse}" Command="{Binding BrowsePreLaunchStartupDirectoryCommand}" Margin="5,5,0,5"
                                AutomationProperties.Name="Browse Pre-Launch Startup Directory"
                                AutomationProperties.AutomationId="ButtonBrowsePreLaunchStartupDir"
                                AutomationProperties.HelpText="Browse to select the pre-launch startup directory."/>
                        <TextBlock Grid.Row="4"  Grid.Column="1" Grid.ColumnSpan="2" Text="{x:Static resx:Strings.Info_PreLaunchStartupDirectory}" FontStyle="Normal" Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" TextWrapping="Wrap"/>
                        <TextBlock Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="2"
                                   Text="{x:Static resx:Strings.Info_StartupDirectory}" 
                                   FontStyle="Normal" 
                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                   TextWrapping="Wrap"/>

                        <TextBlock Grid.Row="6" Grid.Column="0" Text="{x:Static resx:Strings.Label_ProcessParameters}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="6" Grid.Column="1" Grid.ColumnSpan="2"
                                 Margin="0,5"
                                 Text="{Binding PreLaunchParameters, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="Pre-Launch Process Parameters"
                                 AutomationProperties.AutomationId="TextBoxPreLaunchParams"
                                 AutomationProperties.HelpText="Enter command line parameters for the pre-launch process."/>
                        <TextBlock Grid.Row="7" Grid.Column="1" Grid.ColumnSpan="2"
                                   Text="{x:Static resx:Strings.Info_ProcessParameters}" 
                                   FontStyle="Normal" 
                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                   TextWrapping="Wrap"/>

                        <TextBlock Grid.Row="8" Grid.Column="0" Text="{x:Static resx:Strings.Label_EnvironmentVariables}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="8" Grid.Column="1" Text="{Binding PreLaunchEnvironmentVariables, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" Grid.ColumnSpan="2" 
                                 AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Height="100"
                                 AutomationProperties.Name="Pre-Launch Environment Variables"
                                 AutomationProperties.AutomationId="TextBoxPreLaunchEnvVars"
                                 AutomationProperties.HelpText="Enter environment variables for the pre-launch process."/>
                        <TextBlock Grid.Row="9"  Grid.Column="1" Grid.ColumnSpan="2" Text="{x:Static resx:Strings.Info_EnvironmentVariables}" FontStyle="Normal" Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" TextWrapping="Wrap"/>

                        <TextBlock Grid.Row="10" Grid.Column="0" Text="{x:Static resx:Strings.Label_StdoutPath}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="10" Grid.Column="1" 
                                 Margin="0,5"
                                 Text="{Binding PreLaunchStdoutPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="Pre-Launch Standard Output Path"
                                 AutomationProperties.AutomationId="TextBoxPreLaunchStdout"
                                 AutomationProperties.HelpText="Enter the path for the pre-launch standard output log."/>
                        <Button Grid.Row="10" Grid.Column="2" Content="{x:Static resx:Strings.Button_Browse}" Command="{Binding BrowsePreLaunchStdoutPathCommand}" Margin="5,5,0,5"
                                AutomationProperties.Name="Browse Pre-Launch Standard Output Path"
                                AutomationProperties.AutomationId="ButtonBrowsePreLaunchStdout"
                                AutomationProperties.HelpText="Browse to select the pre-launch standard output log file."/>

                        <TextBlock Grid.Row="11" Grid.Column="0" Text="{x:Static resx:Strings.Label_StderrPath}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="11" Grid.Column="1" 
                                 Margin="0,5"
                                 Text="{Binding PreLaunchStderrPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="Pre-Launch Standard Error Path"
                                 AutomationProperties.AutomationId="TextBoxPreLaunchStderr"
                                 AutomationProperties.HelpText="Enter the path for the pre-launch standard error log."/>
                        <Button Grid.Row="11" Grid.Column="2" Content="{x:Static resx:Strings.Button_Browse}" Command="{Binding BrowsePreLaunchStderrPathCommand}" Margin="5,5,0,5"
                                AutomationProperties.Name="Browse Pre-Launch Standard Error Path"
                                AutomationProperties.AutomationId="ButtonBrowsePreLaunchStderr"
                                AutomationProperties.HelpText="Browse to select the pre-launch standard error log file."/>

                        <TextBlock Grid.Row="12" Grid.Column="0" Text="{x:Static resx:Strings.Label_PreLaunchTimeoutSeconds}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="12" Grid.Column="1" Grid.ColumnSpan="2"
                                 Margin="0,5"
                                 Text="{Binding PreLaunchTimeoutSeconds, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="Pre-Launch Timeout"
                                 AutomationProperties.AutomationId="TextBoxPreLaunchTimeout"
                                 AutomationProperties.HelpText="Enter the pre-launch timeout in seconds."/>
                        <TextBlock Grid.Row="13" Grid.Column="1" Grid.ColumnSpan="2"
                                   Text="{x:Static resx:Strings.Info_PreLaunchTimeout}" 
                                   FontStyle="Normal" 
                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                   TextWrapping="Wrap"/>

                        <TextBlock Grid.Row="14" Grid.Column="0" Text="{x:Static resx:Strings.Label_PreLaunchRetryAttempts}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="14" Grid.Column="1" Grid.ColumnSpan="2"
                                 Margin="0,5"
                                 Text="{Binding PreLaunchRetryAttempts, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="Pre-Launch Retry Attempts"
                                 AutomationProperties.AutomationId="TextBoxPreLaunchRetries"
                                 AutomationProperties.HelpText="Enter the number of retry attempts for the pre-launch process."/>

                        <CheckBox Grid.Row="15" Grid.Column="1"
                                  Content="{x:Static resx:Strings.Label_PreLaunchIgnoreFailure}"
                                  IsChecked="{Binding PreLaunchIgnoreFailure, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  Margin="0,5"
                                  AutomationProperties.Name="{x:Static resx:Strings.Label_PreLaunchIgnoreFailure}"
                                  AutomationProperties.AutomationId="CheckBoxPreLaunchIgnoreFailure"
                                  AutomationProperties.HelpText="Check to start the service even if the pre-launch process fails."/>

                    </Grid>
                </TabItem>

                <TabItem Header="{x:Static resx:Strings.Label_PostLaunchTab}"
                         AutomationProperties.Name="{x:Static resx:Strings.Label_PostLaunchTab}"
                         AutomationProperties.AutomationId="TabItemPostLaunch"
                         AutomationProperties.HelpText="Configure a script or process to run after the service successfully starts.">
                    <Grid Margin="20" HorizontalAlignment="Center" VerticalAlignment="Top" MinHeight="520">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="340"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0"  Grid.Column="1" Grid.ColumnSpan="3" Text="{x:Static resx:Strings.Info_PostLaunch}" FontStyle="Normal" Foreground="Black" TextWrapping="Wrap" Margin="5,0,0,5"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="{x:Static resx:Strings.Label_ProcessPathNotRequired}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="1" Grid.Column="1" 
                                 Margin="0,5"
                                 Text="{Binding PostLaunchExecutablePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="Post-Launch Process Path"
                                 AutomationProperties.AutomationId="TextBoxPostLaunchPath"
                                 AutomationProperties.HelpText="Enter the post-launch executable path."/>
                        <Button Grid.Row="1" Grid.Column="2" Content="{x:Static resx:Strings.Button_Browse}" Command="{Binding BrowsePostLaunchProcessPathCommand}" Margin="5,5,0,5"
                                AutomationProperties.Name="Browse Post-Launch Process Path"
                                AutomationProperties.AutomationId="ButtonBrowsePostLaunchPath"
                                AutomationProperties.HelpText="Browse to select the post-launch executable."/>
                        <TextBlock Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2"
                                   Text="{x:Static resx:Strings.Info_ProcessPath}" 
                                   FontStyle="Normal" 
                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                   TextWrapping="Wrap"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="{x:Static resx:Strings.Label_StartupDirectory}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="3" Grid.Column="1" 
                                 Margin="0,5"
                                 Text="{Binding PostLaunchStartupDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="Post-Launch Startup Directory"
                                 AutomationProperties.AutomationId="TextBoxPostLaunchDir"
                                 AutomationProperties.HelpText="Enter the startup directory for the post-launch process."/>
                        <Button Grid.Row="3" Grid.Column="2" Content="{x:Static resx:Strings.Button_Browse}" Command="{Binding BrowsePostLaunchStartupDirectoryCommand}" Margin="5,5,0,5"
                                AutomationProperties.Name="Browse Post-Launch Startup Directory"
                                AutomationProperties.AutomationId="ButtonBrowsePostLaunchDir"
                                AutomationProperties.HelpText="Browse to select the post-launch startup directory."/>
                        <TextBlock Grid.Row="4"  Grid.Column="1" Grid.ColumnSpan="2" Text="{x:Static resx:Strings.Info_PostLaunchStartupDirectory}" FontStyle="Normal" Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" TextWrapping="Wrap"/>
                        <TextBlock Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="2"
                                   Text="{x:Static resx:Strings.Info_StartupDirectory}" 
                                   FontStyle="Normal" 
                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                   TextWrapping="Wrap"/>

                        <TextBlock Grid.Row="6" Grid.Column="0" Text="{x:Static resx:Strings.Label_ProcessParameters}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="6" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,5" 
                                 Text="{Binding PostLaunchParameters, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 AcceptsReturn="False" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Height="80"
                                 AutomationProperties.Name="Post-Launch Process Parameters"
                                 AutomationProperties.AutomationId="TextBoxPostLaunchParams"
                                 AutomationProperties.HelpText="Enter command line parameters for the post-launch process."/>
                        <TextBlock Grid.Row="7" Grid.Column="1" Grid.ColumnSpan="2"
                                   Text="{x:Static resx:Strings.Info_ProcessParameters}" 
                                   FontStyle="Normal" 
                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                   TextWrapping="Wrap"/>

                    </Grid>
                </TabItem>

                <TabItem Header="{x:Static resx:Strings.Label_PreStopTab}"
                         AutomationProperties.Name="{x:Static resx:Strings.Label_PreStopTab}"
                         AutomationProperties.AutomationId="TabItemPreStop"
                         AutomationProperties.HelpText="Configure a script or process to run before the service stops.">
                    <Grid Margin="20" HorizontalAlignment="Center" VerticalAlignment="Top" MinHeight="520">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="340"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0"  Grid.Column="1" Grid.ColumnSpan="3" Text="{x:Static resx:Strings.Info_PreStop}" FontStyle="Normal" Foreground="Black" TextWrapping="Wrap" Margin="5,0,0,5"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="{x:Static resx:Strings.Label_ProcessPathNotRequired}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="1" Grid.Column="1" 
                                 Margin="0,5"
                                 Text="{Binding PreStopExecutablePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="Pre-Stop Process Path"
                                 AutomationProperties.AutomationId="TextBoxPreStopPath"
                                 AutomationProperties.HelpText="Enter the pre-stop executable path."/>
                        <Button Grid.Row="1" Grid.Column="2" Content="{x:Static resx:Strings.Button_Browse}" Command="{Binding BrowsePreStopProcessPathCommand}" Margin="5,5,0,5"
                                AutomationProperties.Name="Browse Pre-Stop Process Path"
                                AutomationProperties.AutomationId="ButtonBrowsePreStopPath"
                                AutomationProperties.HelpText="Browse to select the pre-stop executable."/>
                        <TextBlock Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2"
                                   Text="{x:Static resx:Strings.Info_ProcessPath}" 
                                   FontStyle="Normal" 
                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                   TextWrapping="Wrap"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="{x:Static resx:Strings.Label_StartupDirectory}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="3" Grid.Column="1" 
                                 Margin="0,5"
                                 Text="{Binding PreStopStartupDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="Pre-Stop Startup Directory"
                                 AutomationProperties.AutomationId="TextBoxPreStopDir"
                                 AutomationProperties.HelpText="Enter the startup directory for the pre-stop process."/>
                        <Button Grid.Row="3" Grid.Column="2" Content="{x:Static resx:Strings.Button_Browse}" Command="{Binding BrowsePreStopStartupDirectoryCommand}" Margin="5,5,0,5"
                                AutomationProperties.Name="Browse Pre-Stop Startup Directory"
                                AutomationProperties.AutomationId="ButtonBrowsePreStopDir"
                                AutomationProperties.HelpText="Browse to select the pre-stop startup directory."/>
                        <TextBlock Grid.Row="4"  Grid.Column="1" Grid.ColumnSpan="2" Text="{x:Static resx:Strings.Info_PreStopStartupDirectory}" FontStyle="Normal" Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" TextWrapping="Wrap"/>
                        <TextBlock Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="2"
                                   Text="{x:Static resx:Strings.Info_StartupDirectory}" 
                                   FontStyle="Normal" 
                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                   TextWrapping="Wrap"/>

                        <TextBlock Grid.Row="6" Grid.Column="0" Text="{x:Static resx:Strings.Label_ProcessParameters}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="6" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,5" 
                                 Text="{Binding PreStopParameters, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 AcceptsReturn="False" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Height="80"
                                 AutomationProperties.Name="Pre-Stop Process Parameters"
                                 AutomationProperties.AutomationId="TextBoxPreStopParams"
                                 AutomationProperties.HelpText="Enter command line parameters for the pre-stop process."/>
                        <TextBlock Grid.Row="7" Grid.Column="1" Grid.ColumnSpan="2"
                                   Text="{x:Static resx:Strings.Info_ProcessParameters}" 
                                   FontStyle="Normal" 
                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                   TextWrapping="Wrap"/>

                        <TextBlock Grid.Row="8" Grid.Column="0" Text="{x:Static resx:Strings.Label_PreStopTimeoutSeconds}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="8" Grid.Column="1" Grid.ColumnSpan="2"
                                 Margin="0,5"
                                 Text="{Binding PreStopTimeoutSeconds, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="Pre-Stop Timeout"
                                 AutomationProperties.AutomationId="TextBoxPreStopTimeout"
                                 AutomationProperties.HelpText="Enter the pre-stop timeout in seconds."/>
                        <TextBlock Grid.Row="9" Grid.Column="1" Grid.ColumnSpan="2"
                                   Text="{x:Static resx:Strings.Info_PreStopTimeout}" 
                                   FontStyle="Normal" 
                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                   TextWrapping="Wrap"/>

                        <CheckBox Grid.Row="10" Grid.Column="1"
                                  Content="{x:Static resx:Strings.Label_PreStopLogAsError}"
                                  IsChecked="{Binding PreStopLogAsError, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  Margin="0,5"
                                  AutomationProperties.Name="{x:Static resx:Strings.Label_PreStopLogAsError}"
                                  AutomationProperties.AutomationId="CheckBoxPreStopLogAsError"
                                  AutomationProperties.HelpText="Check to log pre-stop process failures as errors."/>

                    </Grid>
                </TabItem>

                <TabItem Header="{x:Static resx:Strings.Label_PostStopTab}"
                         AutomationProperties.Name="{x:Static resx:Strings.Label_PostStopTab}"
                         AutomationProperties.AutomationId="TabItemPostStop"
                         AutomationProperties.HelpText="Configure a script or process to run after the service stops.">
                    <Grid Margin="20" HorizontalAlignment="Center" VerticalAlignment="Top" MinHeight="520">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="340"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0"  Grid.Column="1" Grid.ColumnSpan="3" Text="{x:Static resx:Strings.Info_PostStop}" FontStyle="Normal" Foreground="Black" TextWrapping="Wrap" Margin="5,0,0,5"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="{x:Static resx:Strings.Label_ProcessPathNotRequired}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="1" Grid.Column="1" 
                                 Margin="0,5"
                                 Text="{Binding PostStopExecutablePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="Post-Stop Process Path"
                                 AutomationProperties.AutomationId="TextBoxPostStopPath"
                                 AutomationProperties.HelpText="Enter the post-stop executable path."/>
                        <Button Grid.Row="1" Grid.Column="2" Content="{x:Static resx:Strings.Button_Browse}" Command="{Binding BrowsePostStopProcessPathCommand}" Margin="5,5,0,5"
                                AutomationProperties.Name="Browse Post-Stop Process Path"
                                AutomationProperties.AutomationId="ButtonBrowsePostStopPath"
                                AutomationProperties.HelpText="Browse to select the post-stop executable."/>
                        <TextBlock Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2"
                                   Text="{x:Static resx:Strings.Info_ProcessPath}" 
                                   FontStyle="Normal" 
                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                   TextWrapping="Wrap"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="{x:Static resx:Strings.Label_StartupDirectory}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="3" Grid.Column="1" 
                                 Margin="0,5"
                                 Text="{Binding PostStopStartupDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="22" MinHeight="22" MaxHeight="22" MaxLines="1" 
                                 TextWrapping="NoWrap" AcceptsReturn="False"
                                 HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                 VerticalContentAlignment="Center"
                                 AutomationProperties.Name="Post-Stop Startup Directory"
                                 AutomationProperties.AutomationId="TextBoxPostStopDir"
                                 AutomationProperties.HelpText="Enter the startup directory for the post-stop process."/>
                        <Button Grid.Row="3" Grid.Column="2" Content="{x:Static resx:Strings.Button_Browse}" Command="{Binding BrowsePostStopStartupDirectoryCommand}" Margin="5,5,0,5"
                                AutomationProperties.Name="Browse Post-Stop Startup Directory"
                                AutomationProperties.AutomationId="ButtonBrowsePostStopDir"
                                AutomationProperties.HelpText="Browse to select the post-stop startup directory."/>
                        <TextBlock Grid.Row="4"  Grid.Column="1" Grid.ColumnSpan="2" Text="{x:Static resx:Strings.Info_PostStopStartupDirectory}" FontStyle="Normal" Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" TextWrapping="Wrap"/>
                        <TextBlock Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="2"
                                   Text="{x:Static resx:Strings.Info_StartupDirectory}" 
                                   FontStyle="Normal" 
                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                   TextWrapping="Wrap"/>

                        <TextBlock Grid.Row="6" Grid.Column="0" Text="{x:Static resx:Strings.Label_ProcessParameters}" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="6" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,5" 
                                 Text="{Binding PostStopParameters, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 AcceptsReturn="False" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Height="80"
                                 AutomationProperties.Name="Post-Stop Process Parameters"
                                 AutomationProperties.AutomationId="TextBoxPostStopParams"
                                 AutomationProperties.HelpText="Enter command line parameters for the post-stop process."/>
                        <TextBlock Grid.Row="7" Grid.Column="1" Grid.ColumnSpan="2"
                                   Text="{x:Static resx:Strings.Info_ProcessParameters}" 
                                   FontStyle="Normal" 
                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                   TextWrapping="Wrap"/>

                    </Grid>
                </TabItem>

            </TabControl>

            <Grid Grid.Row="1" Margin="0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="{x:Static resx:Strings.Label_RequiredFields}"
                           FontStyle="Normal"
                           Foreground="Gray"
                           VerticalAlignment="Center"
                           Margin="10,0,0,0"
                           AutomationProperties.Name="Required Fields Note"/>

                <UniformGrid Grid.Column="1" Rows="1" Columns="5" Margin="0,0,10,0">
                    <Button Content="{x:Static resx:Strings.Button_Install}" Width="80" Margin="5" Command="{Binding InstallCommand}" 
                            AutomationProperties.Name="{x:Static resx:Strings.Button_Install}" AutomationProperties.AutomationId="ButtonInstall" AutomationProperties.HelpText="Installs the configured service."/>
                    <Button Content="{x:Static resx:Strings.Button_Uninstall}" Width="80" Margin="5" Command="{Binding UninstallCommand}"
                            AutomationProperties.Name="{x:Static resx:Strings.Button_Uninstall}" AutomationProperties.AutomationId="ButtonUninstall" AutomationProperties.HelpText="Uninstalls the configured service."/>
                    <Button Content="{x:Static resx:Strings.Button_Start}" Width="80" Margin="5" Command="{Binding StartCommand}"
                            AutomationProperties.Name="{x:Static resx:Strings.Button_Start}" AutomationProperties.AutomationId="ButtonStart" AutomationProperties.HelpText="Starts the configured service."/>
                    <Button Content="{x:Static resx:Strings.Button_Stop}" Width="80" Margin="5" Command="{Binding StopCommand}"
                            AutomationProperties.Name="{x:Static resx:Strings.Button_Stop}" AutomationProperties.AutomationId="ButtonStop" AutomationProperties.HelpText="Stops the configured service."/>
                    <Button Content="{x:Static resx:Strings.Button_Restart}" Width="80" Margin="5" Command="{Binding RestartCommand}"
                            AutomationProperties.Name="{x:Static resx:Strings.Button_Restart}" AutomationProperties.AutomationId="ButtonRestart" AutomationProperties.HelpText="Restarts the configured service."/>
                </UniformGrid>
            </Grid>

        </Grid>

    </DockPanel>
</Window>
